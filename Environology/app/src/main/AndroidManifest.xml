<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sp.com">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/leaf_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/leaf_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Environology">
        <activity
            android:name=".EnvironmentSustainable"
            android:exported="false" />
        <activity
            android:name=".About"
            android:exported="false" />
        <activity
            android:name=".InputMall"
            android:exported="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".EnvironmentMap"
            android:exported="false"
            android:label="@string/title_activity_environment_map" />
        <activity
            android:name=".DeleteRecycle"
            android:exported="false" />
        <activity
            android:name=".CarbonFootprintWebView"
            android:exported="false"
            android:parentActivityName=".Consumer" />
        <activity
            android:name=".BusinessSendMessage"
            android:exported="false" />
        <activity
            android:name=".QRScanner"
            android:exported="false" />
        <activity
            android:name=".ConsumerOrder"
            android:exported="false" />
        <activity
            android:name=".Business"
            android:exported="false" />
        <activity
            android:name=".BusinessLogin"
            android:exported="false" />
        <activity
            android:name=".Consumer"
            android:exported="false" />
        <activity
            android:name=".Login"
            android:exported="false" />
        <activity
            android:name=".CreateAccount"
            android:exported="false" />
        <activity
            android:name=".Home"
            android:exported="false" />
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity" />
    </application>

</manifest>